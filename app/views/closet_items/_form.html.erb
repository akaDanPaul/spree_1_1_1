<%= form_for(@closet_item) do |f| %>
  <% if @closet_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@closet_item.errors.count, "error") %> prohibited this closet_item from being saved:</h2>

      <ul>
      <% @closet_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
